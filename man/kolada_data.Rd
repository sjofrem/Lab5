% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\name{kolada_data}
\alias{kolada_data}
\alias{data_kpi_year}
\alias{data_municipality_year}
\alias{data_kpi_municipality}
\title{KPI data (municipality level) for any combo of kpi / municipality / year}
\usage{
kolada_data(
  kpi = NULL,
  municipality = NULL,
  year = NULL,
  from_date = NULL,
  region_type = NULL,
  page = 1,
  per_page = 5000
)

data_kpi_year(
  kpi,
  year,
  from_date = NULL,
  region_type = NULL,
  page = 1,
  per_page = 5000
)

data_municipality_year(
  municipality,
  year,
  from_date = NULL,
  region_type = NULL,
  page = 1,
  per_page = 5000
)

data_kpi_municipality(
  kpi,
  municipality,
  from_date = NULL,
  region_type = NULL,
  page = 1,
  per_page = 5000
)
}
\arguments{
\item{kpi}{Character vector of KPI ids (e.g., "N00003")}

\item{municipality}{Character vector of municipality ids (e.g., "0180")}

\item{year}{Numeric/character vector of years (e.g., 2019:2026)}

\item{from_date}{Optional "YYYY-MM-DD"…}

\item{region_type}{Optional "municipality" or "region"…}

\item{page}{Page number}

\item{per_page}{Items per page}
}
\value{
Kolada envelope list(values, count, next_page/next_url, previous_*)
}
\description{
KPI data (municipality level)

Convenience wrapper around \code{\link[=kolada_data]{kolada_data()}} for \code{kpi} + \code{year}.

Convenience wrapper around \code{\link[=kolada_data]{kolada_data()}} for \code{municipality} + \code{year}.

Convenience wrapper around \code{\link[=kolada_data]{kolada_data()}} for \code{kpi} + \code{municipality}.
}
